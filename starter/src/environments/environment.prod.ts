// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`
// set to empty string to pull posts via API from the same PROD server 
export const environment = {
  production: true,
  apiEndpoint: ''
};
