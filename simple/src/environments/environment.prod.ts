// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`
// in PROD, API set to empty string to pull posts from the same server
export const environment = {
  production: true,
  apiEndpoint: ''
};
